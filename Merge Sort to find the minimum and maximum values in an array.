N = 8
a = [2, 4, 6, 8, 10, 12, 14, 18]

min_value = a[0]
max_value = a[0]

for i in range(1, N):
    if a[i] < min_value:
        min_value = a[i]
    if a[i] > max_value:
        max_value = a[i]

print("Min =", min_value, "Max =", max_value)
